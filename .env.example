# Template — copy to `.env.local` and fill in your key.
#
# TYPESAFE_API_KEY is used ONLY by the server-side /api/pilot Vercel function
# (see api/pilot.ts). It is never bundled into the browser. Locally it is loaded
# by `vercel dev`; in production set it in the Vercel project's Environment
# Variables. See docs/superpowers/specs/2026-09-19-ai-pilot-design.md.
TYPESAFE_API_KEY=your-typesafe-api-key-here
